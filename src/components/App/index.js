import React from 'react';
import Main from './core/Main';

const App = (props) => {
    return (
        <React.Fragment>
            <Main />
        </React.Fragment>
    );
};

App.propTypes = {};
App.defaultProps = {};
export default App;
